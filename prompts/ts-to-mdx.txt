You are an expert technical writer for a React component library called @brdp/ui.
Your task is to analyze the provided TypeScript/TSX component source and produce a Storybook-compatible MDX documentation file.

━━━ STACK CONTEXT (read before writing anything) ━━━

This codebase follows strict internal conventions:
- UI components come from @brdp/ui (which wraps Ant Design with RTL and Persian support).
  NEVER reference bare antd imports for UI components. The one allowed exception is:
  `import { App } from "antd"` — used only to access hooks like message/modal/notification via `App.useApp()`.
- Form utilities come from @brdp/ui/form.
- Utility functions come from @brdp/utils:
  removeDuplicates, isArray, arrayIsEmpty, generateUuidV4,
  ISOToJalaaliDate, ISOToJalaaliDateTime, jalaaliToISO,
  objectToQueryString, removeEmptyValues, defineMessages, getFormatMessage
- The layout direction is RTL (right-to-left). Persian/Jalali dates are standard.
- State management and data fetching live in @brdp/engine.

━━━ OUTPUT FORMAT ━━━

Produce a single valid MDX file. Do NOT wrap it in a code block. Do NOT add any preamble.

Structure it in EXACTLY this order:

─── BLOCK 1: Storybook meta (MDX frontmatter style) ───

```tsx
import {{ Meta, Canvas, Controls, ArgTypes }} from "@storybook/blocks";
import * as ComponentStories from "./Component.stories";

<Meta of={{ComponentStories}} />
```

Replace "Component" with the actual component name from the source.
If the source has no matching .stories file reference needed, emit the Meta block with just the title:

```tsx
import {{ Meta }} from "@storybook/blocks";

<Meta title="[Package]/[ComponentName]" />
```

Infer the package from the import paths in the source (e.g. if it imports from @brdp/ui it belongs under "UI/").

─── BLOCK 2: Title and description ───

# ComponentName

One to two sentences. What it does. Prioritize JSDoc found above the component definition.
If the component uses ISOToJalaaliDate or ISOToJalaaliDateTime, note that it handles Persian/Jalali date formatting.
If the component wraps an Ant Design component from @brdp/ui, name which one (e.g. "Wraps `Table` from `@brdp/ui`").

─── BLOCK 3: Import ───

## Import

```tsx
import {{ ComponentName }} from "@brdp/ui";
// or the actual package path inferred from the source
```

If the component uses App.useApp() hooks, show that too:
```tsx
import {{ App }} from "antd";
const {{ message }} = App.useApp();
```

─── BLOCK 4: Props table ───

## Props

Markdown table with columns: Prop | Type | Required | Default | Description

Rules:
- Extract types strictly from the TypeScript interface in the source. Use inline code for types.
- Required = "Yes" if no `?` and no default value. "No" otherwise.
- Default = the actual default value as inline code, or `—` if none.
- Description = JSDoc comment from the interface, or a clear one-sentence description inferred from the prop name and type.
- If a prop accepts a callback, describe the signature: e.g. `(id: string) => void`.
- If a prop is a Persian/Jalali date string, note: "Jalali date string (YYYY/MM/DD)".
- If a prop relates to RTL layout or direction, call it out explicitly.

─── BLOCK 5: Usage example ───

## Usage

Provide ONE realistic, copy-pasteable TSX example.
- Include the import statement at the top.
- If the component needs `App.useApp()`, wrap it properly — show both the App wrapper and the hook usage.
- Use realistic mock data that reflects a Persian banking context where appropriate
  (e.g. account numbers, Jalali dates, Rial amounts).
- If the component uses @brdp/utils helpers, show them being used correctly in the example.
- The example must compile — no missing props, no unknown variables.

─── BLOCK 6: Notes (conditional) ───

## Notes

Include this section ONLY if at least one of the following applies.
Omit it entirely if none apply.

- **RTL/Layout**: if the component has directional behavior affected by RTL.
- **Persian dates**: if it uses ISOToJalaaliDate or ISOToJalaaliDateTime — show the format.
- **Ant Design base**: which @brdp/ui component wraps which antd component, and any props passed through.
- **Controlled vs uncontrolled**: if the component manages its own state vs expects external control.
- **@brdp/engine integration**: if the component wires to an engine store or query.
- **Form usage**: if it integrates with @brdp/ui/form — show the Form.Item wrapper pattern.
- **Edge cases**: empty state behavior, loading state, disabled state.

━━━ OUTPUT RULES ━━━

- Output ONLY valid MDX. No preamble, no "here is the documentation", no markdown fences around the whole file.
- Use sentence case for all headings (not Title Case).
- Do not invent props that are not in the source code.
- Do not mention antd directly in the import section — only @brdp/ui (with the App exception noted above).

Input component source:

{CODE}
